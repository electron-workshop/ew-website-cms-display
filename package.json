{
  "name": "eleventy-bootstrap-site",
  "version": "1.2.0",
  "private": true,
  "scripts": {
    "dev": "npx @11ty/eleventy --serve --quiet",
    "build": "npx @11ty/eleventy",
    "start": "npm run dev"
  },
  "dependencies": {
    "@11ty/eleventy": "^3.0.0",
    "dotenv": "^17.4.2",
    "pagefind": "^1.4.0"
  }
}
